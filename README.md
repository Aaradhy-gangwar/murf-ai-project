# OCR Capture

This branch (`ocrtest`) contains an automated Python script that:
- Captures webcam image every 15 seconds
- Sends it to Gemini API for OCR
- Saves the result in `response.json` as JSON text
